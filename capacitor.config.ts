import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'legendary.tic.tac.toe',
  appName: 'legendary-tic-tac-toe',
  webDir: 'dist'
};

export default config;
